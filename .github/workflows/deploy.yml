name: Deploy Fashion Shop Admin

on:
  push:
    branches: [ master, main ]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout Code
      uses: actions/checkout@v3
    
    - name: Install pnpm
      uses: pnpm/action-setup@v2
      with:
        version: 10
    
    - name: Setup Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '20'
        cache: 'pnpm'
    
    - name: Install Dependencies
      run: pnpm install --frozen-lockfile
    
    - name: Build Project
      run: pnpm run build
      env:
        NODE_ENV: production
    
    - name: List Build Files
      run: |
        echo "构建完成！"
        echo "构建文件列表："
        ls -lah dist/
        echo "=========================================="
        echo "index.html 内容预览："
        head -n 5 dist/index.html
    
    # ⬇️ 新增：清理旧文件
    - name: Clean Old Files
      uses: appleboy/ssh-action@v0.1.10
      with:
        host: ${{ secrets.SERVER_HOST }}
        username: ${{ secrets.SERVER_USER }}
        key: ${{ secrets.SERVER_SSH_KEY }}
        script: |
          echo "清理旧文件..."
          sudo rm -rf ${{ secrets.SERVER_PATH }}/*
          echo "清理完成！"
    
    # 部署到服务器
    - name: Deploy to Server
      uses: appleboy/scp-action@v0.1.4
      with:
        host: ${{ secrets.SERVER_HOST }}
        username: ${{ secrets.SERVER_USER }}
        key: ${{ secrets.SERVER_SSH_KEY }}
        source: "dist/*"
        target: ${{ secrets.SERVER_PATH }}
        strip_components: 1
        overwrite: true  # ⬅️ 添加覆盖选项
    
    # 验证部署
    - name: Verify Deployment
      uses: appleboy/ssh-action@v0.1.10
      with:
        host: ${{ secrets.SERVER_HOST }}
        username: ${{ secrets.SERVER_USER }}
        key: ${{ secrets.SERVER_SSH_KEY }}
        script: |
          echo "=========================================="
          echo "部署验证"
          echo "=========================================="
          echo "部署目录内容："
          ls -lah ${{ secrets.SERVER_PATH }}
          echo "=========================================="
          echo "index.html 文件信息："
          ls -lh ${{ secrets.SERVER_PATH }}/index.html
          echo "=========================================="
          echo "index.html 内容预览："
          head -n 10 ${{ secrets.SERVER_PATH }}/index.html
          echo "=========================================="
          echo "设置文件权限..."
          sudo chown -R www-data:www-data ${{ secrets.SERVER_PATH }}
          sudo chmod -R 755 ${{ secrets.SERVER_PATH }}
          echo "=========================================="
          echo "重启 Nginx..."
          sudo systemctl reload nginx
          echo "✅ 部署完成！"
          echo "访问: http://www.yyzzhhh.work.gd"
    
    - name: Deployment Result
      if: always()
      run: |
        if [ ${{ job.status }} == 'success' ]; then
          echo "🎉 部署成功！"
          echo "访问: http://www.yyzzhhh.work.gd"
        else
          echo "❌ 部署失败"
        fi
